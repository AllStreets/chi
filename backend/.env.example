CTA_API_KEY=
TICKETMASTER_KEY=
CTA_BUS_KEY=
OPENWEATHER_API_KEY=
ANTHROPIC_API_KEY=
FRONTEND_URL=http://localhost:5173

# Web push (optional; all three required to enable alerts)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=

# Required for the /api/cron/* endpoints (used by Vercel Cron in production)
CRON_SECRET=

# Overrides the SQLite location (defaults to backend/chicago.db)
SQLITE_PATH=
