VITE_MAPBOX_TOKEN=
# Only needed when the API is hosted separately from the SPA. On the combined
# Vercel deployment leave this unset — the app calls /api on its own origin.
# For local dev against a separate backend process:
# VITE_API_URL=http://localhost:3001
VITE_API_URL=
